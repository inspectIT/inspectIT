package info.novatec.inspectit.jmeter.data;

// NOCHKALL
public class HttpAggregatedResult extends CountOnlyResult {
	public HttpAggregatedResult(long platformId, int count) {
		super(platformId, count);
	}
}
